<!--
  Robot description only (used in private and paired master).
 -->
<launch>
  <arg name="base"/>
  <arg name="stacks"/>
  <arg name="3d_sensor"/>
  
  <arg name="urdf_file" default="$(find xacro)/xacro.py '$(find pi_kobuki_description)/urdf/pi_kobuki_hexagons_laser.urdf.xacro'" />
  <param name="robot_description" command="$(arg urdf_file)" />

</launch>
